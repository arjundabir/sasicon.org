/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "mlazuojtncjivdynfyil.supabase.co",
      },
    ],
  },
};

export default nextConfig;
